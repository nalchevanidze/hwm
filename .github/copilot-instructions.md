Read and strictly adhere to the rules in docs/agents/rules.md. 
Pay special attention to:
1. Resource-Oriented directory structure.
2. Mandatory `stack build` before reporting completion.
3. Usage of `MonadUI` and `MonadError Issue`.