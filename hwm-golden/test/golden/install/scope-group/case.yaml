command: install libs
runner:
  bin:
    cabal: test/mocks/bin/cabal
    stack: test/mocks/bin/stack
    nix: test/mocks/bin/nix
  env:
    HWM_LOG_ID_FIXED: golden
expect:
  calls:
  - args:
    - install
    - bar
    - foo
    - --install-method=copy
    - --installdir
    - .home/.local/bin
    - --overwrite-policy=always
    cabal_project_content: |
      with-compiler: ghc-9.6.6
      packages:
        ../../foo
        ../../bar
    cabal_project_file: cabal-default.project
    tool: cabal
name: install scope-group
notes: |
  Scenario for install/scope-group (command: install libs)
project: simple-bin
