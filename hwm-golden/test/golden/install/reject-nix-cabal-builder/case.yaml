name: install reject-nix-cabal-builder
notes: |
  Scenario for install/reject-nix-cabal-builder (command: install)
project: simple-bin
command: install
runner:
  bin:
    cabal: test/mocks/bin/cabal
    nix: test/mocks/bin/nix
    stack: test/mocks/bin/stack
  env:
    HOME: .home
  path:
    - ${HOME}/.local/bin
expect:
  failure: true
