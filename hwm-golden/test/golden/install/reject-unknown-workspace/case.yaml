command: install ghosts
expect:
  failure: true
name: install reject-unknown-workspace
notes: |
  Scenario for install/reject-unknown-workspace (command: install ghosts)
project: simple-bin
runner:
  bin:
    cabal: test/mocks/bin/cabal
    nix: test/mocks/bin/nix
    stack: test/mocks/bin/stack
  env:
    HOME: .home
  path:
  - ${HOME}/.local/bin
