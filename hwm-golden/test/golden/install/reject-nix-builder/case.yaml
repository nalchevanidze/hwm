command: install
runner:
  path:
    - ${WORKING_DIR}/bin
    - ${HOME}/.local/bin
expect:
  failure: true
name: install reject-nix-builder
notes: |
  Scenario for install/reject-nix-builder (command: install)
project: simple-bin
