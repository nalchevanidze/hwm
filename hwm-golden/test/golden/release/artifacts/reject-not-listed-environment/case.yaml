name: release artifacts rejects non-listed active environment
notes: |
  Active environment is not included in artifact target environments policy.
project: simple
command: release artifacts sample --builder=nix
expect:
  failure: true
