name: workspace add reject-existing-group
notes: |
  Scenario for workspace/add/reject-existing-group (command: workspace add libs)
project: simple
command: workspace add libs
expect:
  failure: true
  files:
