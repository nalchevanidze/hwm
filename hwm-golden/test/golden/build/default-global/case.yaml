command: build
env:
  HWM_LOG_ID_FIXED: golden
expect:
  calls:
  - args:
    - build
    - all
    cabal_project_content: |
      with-compiler: ghc-9.6.6
      packages:
        ../../foo
        ../../bar
    cabal_project_file: cabal-default.project
    tool: cabal
name: build default-global
notes: |
  Scenario for build/default-global (command: build)
project: simple-bin
