name: build fast
notes: |
  Scenario for build/fast (command: build --fast)
project: simple-bin
command: build --fast
runner:
  bin:
    cabal: test/mocks/bin/cabal
    nix: test/mocks/bin/nix
    stack: test/mocks/bin/stack
  env:
    CI: "1"
    HWM_LOG_ID_FIXED: golden
expect:
  calls:
    - args:
      - build
      - all
      - --disable-optimization
      cabal_project_content: |
        with-compiler: ghc-9.6.6
        packages:
          ../../foo
          ../../bar
      cabal_project_file: cabal-default.project
      tool: cabal
