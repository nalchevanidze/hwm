name: build nix-dispatch-shape
notes: |
  Scenario for build/nix-dispatch-shape (command: build)
project: simple-bin
command: build
runner:
  bin:
    cabal: test/mocks/bin/cabal
    nix: test/mocks/bin/nix
    stack: test/mocks/bin/stack
  env:
    CI: 1
    HWM_LOG_ID_FIXED: golden
expect:
  files:
  calls:
    - args:
      - build
      - --no-link
      - .#env-default-all
      tool: nix
