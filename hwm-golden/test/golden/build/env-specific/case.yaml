command: build --env default
runner:
  bin:
    cabal: test/mocks/bin/cabal
    stack: test/mocks/bin/stack
    nix: test/mocks/bin/nix
  env:
    HWM_LOG_ID_FIXED: golden
expect:
  calls:
  - args:
    - build
    - all
    cabal_project_content: |
      with-compiler: ghc-9.6.6
      packages:
        ../../foo
        ../../bar
    cabal_project_file: cabal-default.project
    tool: cabal
name: build env-specific
notes: |
  Scenario for build/env-specific (command: build --env default)
project: simple-bin
