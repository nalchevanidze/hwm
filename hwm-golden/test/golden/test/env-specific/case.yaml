command: test --env default
runner:
  bin:
    cabal: test/mocks/bin/cabal
    stack: test/mocks/bin/stack
    nix: test/mocks/bin/nix
  env:
    CI: "1"
    HWM_LOG_ID_FIXED: golden
expect:
  calls:
  - args:
    - test
    - all
    cabal_project_content: |
      with-compiler: ghc-9.6.6
      packages:
        ../../foo
        ../../bar
    cabal_project_file: cabal-default.project
    tool: cabal
name: test env-specific
notes: |
  Scenario for test/env-specific (command: test --env default)
project: simple-bin
