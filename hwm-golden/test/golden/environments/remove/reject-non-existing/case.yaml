command: environments remove ghost
expect:
  failure: true
name: environments remove reject-non-existing
notes: |
  Scenario for environments/remove/reject-non-existing (command: environments remove ghost)
project: simple
